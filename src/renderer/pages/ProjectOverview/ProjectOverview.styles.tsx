// Minimal styles for ProjectOverview page - no animations, clean dashboard look
export const projectOverviewStyles = `
  .stat-card {
    background: var(--chakra-colors-gray-700);
    border: 1px solid var(--chakra-colors-gray-600);
    border-radius: 8px;
  }

  .metadata-badge {
    background: var(--chakra-colors-gray-600);
    color: var(--chakra-colors-gray-100);
    border-radius: 4px;
    padding: 4px 12px;
    font-size: 0.875rem;
  }
`
